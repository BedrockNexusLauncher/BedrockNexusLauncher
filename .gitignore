# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.so
*.dylib

!/internal/peeditor/PeEditor.exe
!/internal/lip/lipd.exe

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

build/bin
build/versions
logs
bin
native
nda

.idea
.task
.vscode

.claude
.cursor
.windsurf
.agent
.trae
.kiro

.env

build-test.bat
AGENTS.md
node_modules/
docs/.vitepress/cache/
docs/.vitepress/dist/
docs/.vitepress/.temp/
frontend/dist/
.zcode/
.mimosa/

# Internal agent navigation notes (same category as AGENTS.md)
PROJECT_MAP.md

# Generated by scripts/compare_locale_json.py
locale_diff.json

# Private patch payload (dropped in locally for private builds; never committed)
Patch/install.ps1
